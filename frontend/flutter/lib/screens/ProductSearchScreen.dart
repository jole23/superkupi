import 'package:flutter/material.dart';

class ProductSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pretraga Proizvoda'),
      ),
      body: Center(
        child: Text('Ovdje će biti pretraga proizvoda.'),
      ),
    );
  }
} 